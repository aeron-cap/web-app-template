## Quick Starter template for a Dockerized NestJS backend with Drizzle ORM with PostgreSQL
A scalable backend template leveraging the NestJS framework, Drizzle ORM for type-safe database interactions, and a PostgreSQL database, all containerized with Docker.

## Quick Start
```bash
    npm install
```